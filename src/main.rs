/// Remember to replace this crate name with your own
use rust_template::add::*;
/// main function shouldn't have much code
/// just call the functions which define in other modules
fn main() {
  add();
}
